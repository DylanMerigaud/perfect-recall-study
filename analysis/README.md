# analysis

The statistics used throughout (interval estimation, a cluster bootstrap, paired tests,
inter-rater agreement), the script that compares validation protocols against real captures, the
script that scores each preregistered hypothesis by its pre-declared criterion, and the script
that writes the single numbers registry every manuscript figure and table is read from.

Empty until the analysis runs. See the root README for how this folder fits the rest of the study.
