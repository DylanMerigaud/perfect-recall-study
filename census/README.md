# census

The script that lists the population of candidate public repositories by a fixed, written rule;
the sheet of evidence quotes pinned to exact commits; the coding sheets from each coder against a
fixed codebook; and the script that computes inter-rater agreement.

Empty until the census runs. See the root README for how this folder fits the rest of the study.
