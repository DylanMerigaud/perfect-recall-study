# env

The pinned package and tool versions the measurements run under (Python packages, Tesseract,
poppler's pdftoppm, the operating system), recorded so a reader can rebuild the same environment.

Empty until the measurement environment is recorded. See the root README for how this folder fits
the rest of the study.
